<header class="page-header">
  <h1>
    Company:
    <strong><%= @company.name %></strong>
    <%= link_to "Back", exec_company_carrier_path(@carrier), class: "page-header-back-btn" %>
  </h1>
</header>

<h2><%= @carrier.name %></h2>

<p>
  The product <strong><%= @carrier_product.name %></strong> cannot be deleted because it is sold to the following companies:
</p>

<ul>
  <% @child_carrier_products.each do |carrier_product| %>
    <li><%= carrier_product.company.name %></li>
  <% end %>
</ul>
