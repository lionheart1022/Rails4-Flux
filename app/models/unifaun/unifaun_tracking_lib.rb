class UnifaunTrackingLib < TrackingLib
  # Having a super-class like TrackingLib doesn't give us much so instead the implementation is in a custom module.
  # Check out the module UnifaunAutoTracking in app/models/unifaun/unifaun_auto_tracking.rb
end
